version: '3.8'

services:
  app1:
    image: dockersamples/visualizer:stable
    ports:
      - "8081:8080"
    deploy:
      placement:
        constraints:
          - node.role==manger
    volumes:
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock